@model IQueryable<myStore.Domain.Entities.Product>

@{
    ViewBag.Title = "Product Catalogue";
}

<h2>Product Catalogue</h2>

<ul>
@foreach (var product in Model)
{
    <li>
        <h3>@Html.ActionLink(product.Name, "Detail", new { productId = product.Id })</h3>
    </li>    
}
</ul>